default:
  image: python:3.11
  before_script:
    - python --version ; pip --version  # For debugging
    - pip install -q pdm
    - pdm install --no-lock -q --skip=post_install # skip pre-commit hooks

  # Pip's cache doesn't store the python packages
  # https://pip.pypa.io/en/stable/topics/caching/
  cache:
    paths:
      - .cache/pip
      - .venv


# Change pip's cache directory to be inside the project directory since we can
# only cache local items.
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  TEMPLATE_CI_BRANCH: "ci/template"


# Runs jobs only in merge request pipelines or if the branch is TEMPLATE_CI_BRANCH
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_BRANCH == $TEMPLATE_CI_BRANCH


stages:
  - lint
  - test


include: .gitlab/ci/*.yml
