defaults:
  - logger: mlflow
  - data: single_image
  - method: lookahead
  - model: resnet50
  - classes: imagenet
  - replacement: mean_color
  - segmentation: hexagonal
  - _self_

# Explainer hyperparameters
target_class_idx: null
max_regions: 1
sigma: null
desired_length: 30
batch_size: 64
seed: 42
