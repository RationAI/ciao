defaults:
  - logger: mlflow
  - method: lookahead
  - model: resnet50
  - classes: imagenet
  - replacement: mean_color
  - segmentation: hexagonal
  - _self_

# Explainer hyperparameters
image_path: ??? # Must be provided via CLI
target_class_idx: null
max_regions: 1
desired_length: 30
batch_size: 64
