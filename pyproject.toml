[project]
name = "rationai-"
version = "0.1.0"
description = ""
authors = []
requires-python = ">=3.11"
readme = "README.md"
license = { file = "LICENSE" }
dependencies = []

[dependency-groups]
dev = ["mypy", "ruff"]
test = ["pytest", "pytest-cov"]
